export default function (value: string) {
    return `
        Generate 5 hashtags in Portuguese for a post about ${value}.
        Return each item separated by a comma, in lowercase, and without a line break.
    `;
}